# Agents Sleep Preventer 4.0.2

- Fix Codex hook handling so `asp start` disables sleep immediately when Codex begins working.
- Re-enable sleep immediately from `asp stop` when the last working agent finishes.
- Keep hook transition logging quiet unless the sleep state actually changes.
