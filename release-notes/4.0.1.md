# Agents Sleep Preventer 4.0.1

- Update Codex integration to enable `hooks = true` in `~/.codex/config.toml`.
- Remove the deprecated `codex_hooks` setting during hook installation to avoid Codex startup warnings.
- Keep existing Claude Code and Codex hook cleanup behavior intact.
