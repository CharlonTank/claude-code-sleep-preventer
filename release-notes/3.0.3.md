# Agents Sleep Preventer 3.0.3

- Fix login startup so macOS opens the app bundle instead of launching the legacy CLI menubar directly.
- Keep `/usr/local/bin/asp` synchronized with the installed app binary during installer flows.
- Update package metadata versioning so future installs are not treated as stale package receipts.
