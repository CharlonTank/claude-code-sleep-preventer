# Agents Sleep Preventer 4.0.0

- Rename the app, CLI, bundle identifier, installer metadata, and release feed from Claude Code Sleep Preventer to Agents Sleep Preventer.
- Add Codex support with `~/.codex/hooks.json` installation and Codex process detection.
- Keep the menu bar counter working for Claude Code and Codex sessions.
- Publish Sparkle updates from the `CharlonTank/agents-sleep-preventer` release feed.
